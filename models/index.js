const User = require('./user.model')
const Category = require('./category.model')
const Article = require('./article.model')
const Comment = require('./comment.model')

module.exports = { User, Category, Article, Comment}